[pytest]
addopts = -q --import-mode=importlib
markers =
    unit: fast, in-process unit tests (no live services)
    smoke: quick sanity checks against a deployed stack
    integration: multi-component integration tests
    live: end-to-end tests that require live data sources (local services, Snowflake, etc.)
